/* global hasher */
sap.ui.define(['sap/fe/core/AppComponent'], function (AppComponent) {
  'use strict';

  return AppComponent.extend('com.sap4kids.approver.Component', {
    metadata: {
      manifest: 'json'
    }
  });
});
